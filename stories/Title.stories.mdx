import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs';
import {Title} from '../src';

<Meta title="COMPONENTS/Title" component={Title} />

export const Template = (args) => <Title {...args}/>;

# Title

A title component applies top and bottom margins, font weight, font size, and line height to titles.
The most common use for a title is to define headers within a page.
See the Typography Guidelines for more information on the relationship between title component sizes and HTML header levels.

<Canvas>
    <Story
        name="Heading"
        args={{
            children: 'Heading level',
            headingLevel: 'h1'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Heading" sort/>
